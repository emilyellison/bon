<% provide(:title, "Welcome #{@user.name}." ) %>
<div class='row'>
	<aside class='span4'>
		<section>
			<h1>
				<%= gravatar_for @user %>
				<%= "Welcome #{@user.name}." %><br/>
			</h1>			
		</section>
	</aside>
</div>
